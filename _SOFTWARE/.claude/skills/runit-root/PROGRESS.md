# runIT — Progress Log

Last updated: 2026-09-24 · branch `vm_ui` (devkit + PCB test sessions 2026-09-24 not committed yet)

Status legend: 🟡 in progress · ⏳ planned · ⛔ blocked. Finished items move to the done log as one line; their design lives in the skill files and component docs.

## runit-esp32 (firmware)

| Area | Status | Notes |
|---|---|---|
| Board test | 🟡 | **Devkit done 2026-09-24** (GPIO only): interface / responses / errors, GPIO, device lifecycle, actions, events → action, connectors, VM, persistence, BLE — all suites pass (`runit-esp/build.md` §6). **PCB stage 1 done 2026-09-24** (COM4, native USB): GPIO, TCA6424A (LEDs, freeze / sync, inputs), ADS7128 (voltages, window alerts → event → action), PCA9685, AP33772S (VBUS 5.04 V, 144 mA; no PD offers from the current source) — `pcb_tests.py` passes. **Next: stage 2** — TPS55289 rails, INA3221, power manager on real readings, then H-bridge. **Stage 2 (2026-09-24, in progress):** all 8 devices install; INA3221 channels measured (rail B / 0x75 = ch0, input = ch1, rail A / 0x74 = ch2; input shunt reversed by design → `inverted_mask`); both rails regulate at 5 V, OCP alerts armed on ch0 / ch2. **Blocker:** raising a rail above ~5 V power-cycles the whole board (reset reason power-on) on the PC USB feed — worked twice earlier, then every time, with any budget and with a 200 mA soft rise; INA ch2 shows 0 mA. Needs a hardware look before 10 V and the short test. Temp in firmware: `tps_diag` logs (`adapter_tps55289.c`), `RUNIT_BOARD_TEST_UNKNOWN_SOURCE_MA` (0 = off, `runit_board_cfg.c`) |
| Power manager | 🟡 | Built (`runit-esp/architecture.md` §4.9, SYS_POWER.MD). To confirm: INA3221 channel order, source indicator pins, reserve / efficiency estimates (`runit_board_defs.h`, `runit_board_cfg.c`). USB-PD uses the offered, not the negotiated current |
| Events (`sys_event`) | 🟡 | Built, not tested on the board (§4.11, SYS_EVENT.MD). Device JSON doesn't list the events each device publishes (no annotation directive yet) |
| Error handling | 🟡 | Open: hbridge error map with `dev_id` in `device_id_of()`; i2c bus-recovery hook (`runit-esp/errors.md` §2) |
| DRV8962 H-bridge device | 🟡 | #2 (ID 6, PCA CH 8-15) on the board and PWM measured OK 2026-09-24. #1 (ID 5) configured but off (`RUNIT_BOARD_DEV_DRV8962_0 0`): the ESP GPIO adapter has no PWM (`set_pwm_*` = NULL) - add LEDC PWM first. Missing: install decoder + annotations + JSON (`runit-esp/devices.md` §4). Driver notes: VREF is one pin per chip and follows the largest channel limit; the fault latch is software-only (`set_drive` doesn't check `is_fault`); a rail rising through VM UVLO (~4.3 V) can post one THERMAL_OR_UVLO event; IPROPI resistor assumed 3.09 kΩ (unconfirmed); TCA 7 (OCPM, both chips) left undriven |
| Device adapter fixes | ⏳ | GPIO ESP safe suspend (`devices.md` §5.1); TPS55289 no I2C probe; TCA6424A extra `_new`/`_delete` exports |
| Freeze reconsideration | ⏳ | Write → read-back chains can't complete while frozen; options and coverage gaps in `devices.md` §5.2 |
| Features analysis | ⏳ | On hold (architecture.md §4.4): F-2 type confusion, F-17 lifecycle/fault handling |
| Naming cleanup | ⏳ | `runit-esp/conventions.md` §5.2 |
| `utils.h` static allocator fixes | ⏳ | `runit-esp/conventions.md` §1 (linkage, double start, stack param, buffer capacity, …) + hard-coded stacks → Kconfig |
| Devkit test findings | ⏳ | From 2026-09-24: (1) GPIO ESP descriptor offers `OUTPUT_OPEN_DRAIN_PULLUP`, the adapter refuses it (`ERR_IO_PIN_MODE_UNSUPPORTED`) — drop it from the device's `one_of` or support it; (2) IO on an unconfigured pin reports `ERR_IO_PIN_FEATURE_UNSUPPORTED` (should say "not configured"); (3) `0x43` set data with an unknown object ID reports `ERR_VM_ACCESSOR_UNKNOWN_ID` (name says accessor); (4) `bind_tx` / `bind_rx` accept a bad endpoint, it fails only on send (no provider `check_endpoint` op); (5) every decoder logs one INFO line per command (`reading io level ...`) — noise on the logs stream; (6) VM G-10 (overrun after pause, `runit-esp/vm/findings.md`); (7) F-GAP-4 confirmed: recording executes the frames. From the PCB: (8) TCA6424A `set_mode` on a pin already in use (board LED 22) succeeds, the ESP GPIO adapter refuses the same (`ERR_IO_PIN_ALREADY_IN_USE`) — pick one rule for all IO adapters, and protect board-owned pins; (9) pins locked by a device (TCA 0 = PCA /OE, GPIO 42 = ADS ALERT) refuse even `get_level` (`ERR_IO_PIN_LOCKED`); (10) PCA9685 duty max is the unresolved symbol `PCA9685_MAX_PWM_VALUE` in its device JSON, and 4096 is accepted; (11) the power manager starts before boot action 1 installs the devices (first INA read: `device 12 is not registered`); (12) a budget refusal surfaces as the TPS range error `ERR_INVALID_VAL_UI32` (naming the other rail when rebalancing) instead of a budget error; (13) on a 5 V source with an unknown limit (1 A assumed) only ~6 V fits one rail — two rails at 10 V need ~8.1 W; (14) BLE advertising after a drop — **retested 2026-09-24 on USB with the serial log: not reproduced** (clean close, killed client: Windows holds the link ~13 s then HCI 0x13 + re-advertise, 4-min hold without drop). The board doesn't advertise while a stale host process still holds the link. Advertising restart failures are now reported (were released silently); if it recurs, the log shows why; (15) **TPS55289 has no active pull-down when a rail steps down**: unloaded rails stayed at ~19 V after 20 → 6 V (PFM can't sink) — use FPWM while stepping down, and DISCHG before switching a rail off; (16) TPS55289 once didn't answer I2C 10 ms after EN high (output pre-charged to 3.3 V) → `ESP_ERR_INVALID_RESPONSE` on enable; a retry worked — longer EN settle or retry the first access; (17) DAC53202 driver used wrong registers (config at 0x01, data at 0x1C/0x1F) — fixed per datasheet (data 0x1C ch0 / 0x19 ch1, COMMON-CONFIG 0x1F, VDD ref); found at 0x48. PCA9685 prescale wrapped to 255 (~24 Hz) for > 1.5 kHz requests — now clamped. DRV8962 #2 (ID 6) up via PCA CH 8-15: VREF 1.31 V (2 A), ch0 +0.5 accepted on rail A 5 V; VM now switched in via LM73100 TCA 12 (rail A). **Measured by the owner 2026-09-24: PWM works** - ch0 +0.5 at 5 V gives one output at 5 V and the other at 2.5 V average (1 kHz). Looks inverted but is the slow-decay drive: IN1 held high, IN2 switched at (1 - magnitude), so the load sees OUT1 - OUT2 = magnitude x VM on average. Legacy pin header fixed: DRV2 nSLEEP/nFAULT = TCA 8/10 (not 9/11), AP33772S INT = TCA 21 (was 12, an LM73100 enable). Rail B refused on the 5 V/1 A USB budget (share 122 mA < TPS 200 mA floor); **PCB 2026-09-24 evening:** (18) power manager polls the INA3221 before boot action 1 installs it → `ERR_DEV_NOT_FOUND device 12` on every boot - start polling after device init, or treat a missing monitor as not-yet-ready; (19) rebalance gives a rail a share below the TPS55289 200 mA floor on a small budget (5 V/1 A USB: 122 mA) → `set_current` fails with `ERR_INVALID_VAL_UI32` and the rail can't be enabled - refuse cleanly (budget error) or clamp / don't enable, instead of a driver range error; (20) source indicator pins known: eFuse PG TCA 3 = VUSB_OK, TCA 4 = VEXT_OK - not wired to `source_pins` (VUSB_OK is also high on plain 5 V USB, and see 18: the manager starts before the TCA6424A); (21) the 2026 pin spreadsheet is outdated (ESP column: I2C, TCA INT/RST, ADS alert; TCA column: DRV sleep/fault swapped, AP33772S INT) - the legacy firmware header is the reference, recorded in HARDWARE.md §3; (22) AP33772S INT was on TCA 12 (actually an LM73100 enable) - moved to TCA 21, PD interrupt path not yet exercised; (23) PCA9685 frequency is shared: H-bridge #2 needs ~1 kHz, servos on CH 0-7 want ~50 Hz - they can't be used together at their natural rates (design constraint; the app must warn); (24) DAC53202 full scale assumes VDD = 3.3 V (VDD reference, gain 1x); VREF pin 16 wiring unknown, not used; (25) `sys_io_set_mode` fails on devices without `set_mode` (PCA9685, DAC53202) - the DRV8962 driver works around it (`prepare_pin`); decide whether fixed-function outputs should accept their natural mode instead |
| Boot-log findings | ⏳ | Flash size 2 MB configured vs 16 MB on board; CPU 160 MHz (240 possible) (`runit-esp/build.md` §2a). The double "Advertising started" is a restart to apply the device name set after sync (cosmetic) |
| Component docs missing | ⏳ | `sys_i2c`, `sys_hbridge` have no `.MD` (ask the user for purpose first, conventions.md §8) |
| Data connector rework | 🟡 | Verified on the devkit 2026-09-24 (BLE from Windows + UART). UART provider built (SYS_UART.MD). Open: Wi-Fi / MQTT providers |
| App: data connector IDs | ⏳ | Connector / provider IDs are published in `enums.json` (`sys_data_connector_id_e`, `runit_data_provider_e`) and referenced by the class `0x06` fields (`enum_ref`); the app doesn't consume `enums.json` yet |
| App: VM program compiler | ⏳ | Build programs from `vm-program.generated.json` (records, widths, limits, `total_size` formulas, telemetry) + `vm/blocks/*.generated.json` (per block: pins and value kinds, activation, load rules, state layout, EXPR opcodes and golden vectors) + `vm-model.generated.json` (object header) + `enums.json`; create a VM-routed event subscription (class `0x09`) for programs with ON_EVENT blocks |
| VM core | 🟡 | 15 blocks on one palette table (`g_vm_block_types`), one descriptor per block in `data-structures/vm/blocks/`. Open (`runit-esp/vm/findings.md`): test target (G-5, needs a toolchain decision), program storage on the device (G-8, next), NVS / flash size (G-9). Latch/periodic/action/on_event not run on hardware |
| Features: servo, hbridge | 🟡 | Skeletons (`components/features/`) |
| Headers cleanup | 🟡 | Part 1 done; packet defines moved to Kconfig |
| H-bridge power-source selection (LM73100) | ⏳ | Pins known (legacy header): TCA 12 DRV2←rail A, 13 DRV2←VSUP, 14 DRV1←VSUP, 15 DRV1←rail B; all driven low at boot (`runit_board_cfg.c`). Missing: an API / device so the app picks one source per H-bridge, with an interlock (never both switches of one DRV on), budget accounting for VSUP-fed bridges, and switch-off on rail faults. Today only raw `sys_io_set_level` on the TCA pins |
| ADS7128 digital-input mode | ⏳ | ADC read mapped to a level by threshold (below 0.5 V low, hysteresis TBD); regenerate JSON |
| Wi-Fi coexistence | ⏳ | Not implemented |
| User transports (LoRa, sub-GHz) | ⏳ | Concept only |
| Host tests | ⏳ | Toolchain proven: `zig cc` via pip, 32-bit target (`runit-esp/vm/findings.md` G-5). A one-off `vm_sub` run found and fixed 2 bugs; tests deleted afterwards. Open: whether to keep a host test directory in the repo |

## runit-app

| Area | Status | Notes |
|---|---|---|
| BLE backend (Web Bluetooth adapter, GATT catalog) | 🟡 | `app/src/backend/ble/`, test UI `BleTestApp.tsx` |
| Packet packer + data stream / outgoing router | 🟡 | `app/src/backend/packetPack/`, `stream/` |
| Command responses (stream 0x05) | ⏳ | Every command must start with a `seq` byte: `[seq][class][packet][payload]` (`packPacket` doesn't add it yet; a raw-hex test UI must too). Decode `[0x05][seq][class][packet][status][data]`, match pending commands by `seq` with a timeout, OK data via the contract's `response` layout, errors as `u16 tag, u16 owner` (`contracts.generated.json` → `response_stream`, `response`) |
| Power page | ⏳ | Status (`0x3C` response), PSU / battery settings (settings class `0x08`), response matrix (`0x3D`), monitor alerts (`0x39`); enums `sys_power_source_e`, `sys_power_response_e`, `sys_power_battery_e` |
| Event subscriptions page | ⏳ | Class `0x09` (`events` catalog): subscribe returns an ID, unsubscribe by ID / 255 = all; enums `sys_event_domain_e` + per-domain event enums |
| Consume generated JSON descriptors | ⏳ | from `data-structures/*.generated.json` |
| Layout (topbar, sidebar, bottombar, tabs) | ⏳ | Designed in legacy docs `00–14_step_*` (backup: `../_agents_backup_2026-09-22/.agents/skills/runit-app-design/docs/`) |
| Devices view, hardware presence, relations | ⏳ | design docs 07–09 |
| Blocks palette, search, object tree, flow canvas | ⏳ | design docs 10–13 |
| Board pinout SVG, remote controller, telemetry/recorder | ⏳ | design docs 04–06 |
| Capacitor Android build | ⏳ | Web-first, mobile later |

## Repo / tooling

| Item | Status | Notes |
|---|---|---|
| `runit-app` skill | 🟡 | Target features digested with decisions and accepted stack: `runit-app/features.md` (2026-09-24). Rest of the skill still a stub |
| Migrate legacy skill content | ⏳ | From `../_agents_backup_2026-09-22/`; legacy firmware skill has absolute links to another machine (`C:/Users/krolp/...`) |
| JSON annotations → `data-structures/` | 🟡 | Device, contracts, settings, VM model generators + schemas |

## Done log

- 2026-09-24 — DAC + H-bridge #2 up: DAC53202 driver rewritten to the datasheet (was writing config to 0x01 and data onto COMMON-CONFIG 0x1F; now data 0x1C ch0 / 0x19 ch1, COMMON-CONFIG 0x1F power-down per channel, VDD reference; outputs Hi-Z until set), found at 0x48 (A0 = GND). PCA9685 prescale wrapped to 255 (~24 Hz) for requests above ~1.5 kHz - now clamped 3..255 with the 4096 divisor. DRV8962 #2 created (ID 6): IN = PCA CH 8-11, EN = CH 12-15, nSLEEP / nFAULT = TCA 8 / 10, VREF = DAC OUT1 (2 A → 1.31 V), 1 kHz; driver: skips modes on fixed-function pins, VREF programmed at start, tWAKE wait. LM73100 switches (TCA 12-15) off at boot; AP33772S INT fixed to TCA 21. Measured: VM via TCA 12 from rail A at 5 V, ch0 +0.5 → one output 5 V, the other 2.5 V average (slow decay). BLE: re-advertising retested (finding 14), advertising restart failures now reported. Scripts: `ble_readv_test.py`, `ble_hold_test.py`, `hbridge_test.py` (`stop` brakes, opens the VM switch, rail A off), `tca_scan.py`.

- 2026-09-24 — Power faults visible and rails stable: `ERR_POWER_FAULT` (MEDIUM, device / channel / event / value / response) raised by the power manager for every OVP … OTP event, so faults reach the app's error stream; TPS55289 slew set to the slowest 1.25 mV/µs, soft rise at 500 mA, current-limit floor 500 mA (below ~5 mV sense the chip offset dominates: rail A sat in current limit at 200 mA and flagged OCP), OCP ignored while an output rise settles (cleared after, a persisting overload re-flags); power-manager rebalance skips a suspended rail instead of failing the other rail's change; INA3221 `inverted_mask = 0x07` (all shunts reversed by design; its hardware alerts are refused). Verified on COM4: both rails 6 → 10 V clean (rail A previously reset the board at 9–11 V), faults reported as errors. Earlier over BLE on 9 V PD: rail B 18 V clean, SCP / OCP shorts disabled the rail and the board stayed up. Temp left: `TPS_TEST_CURRENT_CAP_MA` 350.

- 2026-09-24 — TPS55289 driver checked against the datasheet (SLVSGA9A, `tps55289.pdf` in `_SOFTWARE`): fault indication was disabled (CDC mask bits are 1 = indicate; driver wrote 0) → `tps55289_set_fault_reporting()` replaces `set_fault_masks`, CDC now 0xE0; read-modify-write setters no longer write a register built from a failed read (a lost VOUT_FS read would have programmed ~21 V for 10 V); external feedback refused (`ESP_ERR_NOT_SUPPORTED`, VOUT depends on the divider). Verified: REF / IOUT_LIMIT / MODE / STATUS encodings and addresses match. Short tests (5 V / 3 A PD): at a 250 / 350 mA limit a rail short is survived (rail → ~0.1 V, recovers); at ~1.15 A the board power-cycled. Measured short current ≈ 0.65 × the set limit (current-limit accuracy is specified only at 30 / 50 mV sense). All three INA3221 shunts read negative under load (rails too, not only the input).

- 2026-09-24 — PCB stage 2 (partial): device switches in-file (`RUNIT_BOARD_DEV_*`); INA3221 channel map fixed (the old map read rail A as the input → 1.2 W budget → both rails suspended); INA3221 `inverted_mask` (reversed input shunt: sign flipped, its OCP alert refused because it could never trip); TPS55289 adapter: hardware EN has priority (off = EN low), all registers restored on every enable / resume, settings cached while off, default current limit 200 mA (was 100, below the minimum), minimum-limit soft rise on every voltage increase; boot log prints the reset reason (the ROM's `rst:` is lost on native USB); `runit_link` reconnects after a USB drop.

- 2026-09-24 — PCB stage 1 (COM4): per-device in-file boot switches `RUNIT_BOARD_DEV_*` (`runit_board_cfg.c`) replace the `RUNIT_SKIP_DEVICE_INIT` Kconfig option; `sys_uart` also reads `#R:` lines from the native USB (USB-Serial-JTAG secondary console); `pcb_tests.py`, `usb_capture.py`, `Link` reopens the port after a USB reset. Fixed: ADS7128 in-band (INSIDE) window alerts never fired — in-band the chip flags HIGH_TH < code < LOW_TH, so the bounds swap registers; the exit watch after an in-band entry now watches both sides.

- 2026-09-24 — Devkit hardware test session (COM3, GPIO-only devkit): console UART command link (`sys_uart`, `#R:<hex>` lines, provider ID 2, `CONFIG_RUNIT_UART_CONSOLE_LINK`) + host scripts (`runit-esp/scripts/`: `runit_link.py`, `devkit_tests.py`, `vm_tests.py`, `persist_tests.py`, `ble_tests.py`). Fixed: BLE MTU reset to 23 on connect when the client's exchange finished first (Windows: every BLE frame capped at 20 B); `RUNIT_SKIP_DEVICE_INIT` now still installs the ESP GPIO device; blank line after every error-chain log line on serial.

- 2026-09-22 — Root skill, VISION, HARDWARE, PROGRESS; old agent files stripped (backup `../_agents_backup_2026-09-22/`).
- 2026-09-22 — Architecture review; explicit wiring from runit; `sys_errors` sink injection + error-map folders; `SE_MUST_USE` and all dropped errors fixed.
- 2026-09-22 — Contracts / dispatch / units / dead code (F-13/14/16/18); const vtables + per-instance pin locks; device ID on every device error; no driver/adapter logs.
- 2026-09-22 — Faults → suspend; onboard device protection; owners per module (F-4); settings store (`sys_settings`); command responses (F-12); power manager built.
- 2026-09-22 — Flash + serial capture on COM3 (`runit-esp/scripts/serial_capture.py`).
- 2026-09-23 — Error handling review (severity, async log task, stable tag IDs, renames), F-11 wiring, repeat suppression.
- 2026-09-23 — Events (`sys_event`, F-15) replace callbacks; subscribe / unsubscribe packets (class `0x09`).
- 2026-09-23 — VM blocks published per block (G-2 done): `data-structures/vm/blocks/` + index, with `@activation`, pin `@value` kinds, `//@rule` load checks, derived / runtime state fields; EXPR bytecode checked once at load (`vm_expr_check`, opcode tables `vm_expr_ops` / `vm_bit_ops`), evaluator no longer re-checks it, `VM_BLK_RT_CFG_BAD` removed; opcode table and golden vectors published (vectors verified on the real evaluator in a one-off host run, deleted after). Build-time checks for the VM telemetry / loader constants and distinct stream bytes. Fixed: `block_idx` is a label, blocks run in ID order.
- 2026-09-23 — VM program wire format published (G-2 part 1): `vm_wire.h` packed records used by decoder, loader and telemetry; `vm-program.generated.json` + schema + `generate-vm-program.py` (packets, rules with error tags, index-step union, telemetry frames, type widths, constants, Kconfig limits, `//#vm-arena` formulas); `en_mode` / `on_error` are published enums and validated; accessor paths must end exactly at `idx_len`; vm-model no longer publishes the in-memory accessor / index.
- 2026-09-23 — VM telemetry: sends on value change (quiet writes too) instead of `upd`; snapshot on subscribe (at once if stopped); heap object IDs carry `0x8000` (was a bug) and are described with `0x42` records; large objects split by `start_idx`; lost frames resent (rest of the pass held back after a refusal); oversized subscribe refused; host-tested; `CONFIG_VM_SUB_MAX_TRACKED`, owner `OWNER_VM_SUB`.
- 2026-09-23 — VM retained values: `retentive` objects kept by name in one `sys_settings` record, saved every 60 s (changed only) off the VM task, on teardown and safe state; restored on the first start; `0x48 0A` forgets them; `sys_settings_load_blob()`.
- 2026-09-23 — VM structure: one palette entry per block type (`vm_block_type_t`, shape checked once at load, no per-pass re-check); per-call fault bit replaces the global `g_vm_block_fault`; `ON_EVENT` block; block catalog generator `generate-vm-blocks.py` (+ schema), block enums and `vm_exec_command_e` published.
- 2026-09-23 — VM patches: B-1 fixed (a block rejected by its verify is removed again, `vm_store_undo`); LATCH, PERIODIC, ACTION blocks; `sys_actions_request()` queue so the VM never runs an action in a pass; `@verified` markers removed; VM.MD / VM_EXEC.MD brought up to date (status table), perf history moved to VM_PERF.MD, stale self-test/benchmark references removed.
- 2026-09-23 — `runit-esp` skill complete: VM subfolder `vm/` (README, blocks, wire, findings) from a review of `components/VM`.
- 2026-09-23 — Command sequence byte: `[seq][class][packet]…`, echoed in responses; contracts JSON `matching: seq`.
- 2026-09-23 — Data connector made transport-agnostic: ID-based API, `uint32` endpoints, provider-owned framing, responses to the frame's origin, dynamic frame limit (BLE MTU) instead of truncation, protected system connectors, BLE provider moved into `ble`, `SYS_DATA_CONNECTOR.MD`.
- 2026-09-23 — Legacy `Python/` tooling deleted (class resolution moved to `data-structures/auto-annotations/packet_classes.py`); root `auto-annotations/` removed.
